const entityReducer = (state = {}, action) => {
  Object.freeze(state);
  switch (action.type) {
    // case CASE:
    //   return state;
    default:
      return state;
  }
};

export default entityReducer;